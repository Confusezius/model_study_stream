from setuptools import setup, find_packages

setup(
    name='visual-study',
    version='0.0.1',
    description='',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'tqdm',
    ],
)
